# AG-95F ControllerLoopSpine Source-Class Trace Demotion

Status: historical setup for the AG-95I packet-field diet.

No live ScryRaven/proplex provider, model, search, retrieval, secret, `.env`,
DB row, raw provider payload, raw prompt, private log, cache, full raw trace,
local output packet, or private artifact access was used.

## Current Routing

Source-class recovery dispatch authority remains:

```text
AuthorityLifecycle.recovery_action
-> SourceClassRecoveryRunner
-> SourceClassRecoveryExecutor
```

AG-95F originally marked ControllerLoopSpine source-class packet fields as
diagnostic compatibility, not runner dispatch authority. AG-95I supersedes that
temporary demotion contract by retiring the source-class-specific packet
aliases/markers and routing current guidance through the canonical runner path.

## Historical Record

AG-95F was useful as an intermediate stop: it made the old packet fields
visibly non-authoritative while downstream consumers were still being retired.
Do not restore those retired fields as source-class runner dispatch authority.
Use `docs/architecture/AG95I_CONTROLLER_LOOP_SPINE_PACKET_FIELD_DIET.md` for the
current packet inventory and deletion status.
