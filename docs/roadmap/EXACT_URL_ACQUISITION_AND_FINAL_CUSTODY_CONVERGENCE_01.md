# EXACT-URL-ACQUISITION-AND-FINAL-CUSTODY-CONVERGENCE-01

Status: blocked pending provider-operation eligibility
Mode: BUILD
Proof class: offline_product_path_proof
Depends-on: RUNKERNEL-ACQUISITION-CONTROL-FOUNDATION-01,
INITIAL-DISCOVERY-SELECTIVE-FETCH-RETIREMENT-01,
DISCOVER-RESULT-CANDIDATE-HANDOFF-CONVERGENCE-01, and
EXACT-URL-NETWORK-TARGET-SAFETY-OWNER-01
Predecessor-runtime/test: 48a309124764d813cf27081bf5871d5a9612db79
Starting-runtime/test: 6fbca602afac5a00bb6bafa2a6888b6ec31d5065
Does-not-authorize: live calls, initial DISCOVER transport or planning redesign,
planner disambiguation, Map selection, Crawl custody, general Deep, provider-
failure fallback, or citation/final-answer authority by assertion

## Activation Blocker

This Build must not reactivate until at least one concrete provider operation
is truthfully eligible for the `untrusted_exact_url` target class under the
installed code-owned eligibility policy. Current repository evidence
establishes no Linkup Fetch or Tavily READ, Focused Extract, Map, or Crawl
operation as eligible because sufficient committed public-target guarantees or
observable final-target lineage are not established.

That posture does not mean that Linkup or Tavily is inherently unsafe. It means
only that ScryRaven cannot truthfully assert production eligibility from its
committed guarantees and observable target facts. Provider availability,
preference, adapter installation, or an offline eligibility fixture cannot
satisfy this blocker.

## Outcome

Converge every genuinely ordinary-product-consumed exact-URL acquisition path
for separately justified post-selection READ and FOCUSED_EXTRACT material under
one RunKernel post-discovery authority chain and one bounded final-custody
continuation. Initial DISCOVER material remains provider-returned candidate
material with zero separate candidate-URL transport. Remove or subordinate
post-selection bypasses so the ordinary consumer cannot acquire the same exact
URL through competing policy or execution authority.

The completed product path must reach the existing main-RunKernel
candidate/source/semantic/ComponentCoverage consumer in offline composition.
It must not claim default live CLI activation unless the phase separately makes
and proves that product decision.

## Installed Starting Point

- `core.run_kernel`, `core.acquisition_control`, and
  `core.authorized_acquisition_runtime` own the complete proposal-to-custody
  control chain.
- `core.network_target_safety` is the pure owner for static target parsing,
  typed resolver snapshots, and admission, final-pretransport, and
  posttransport observed-target decisions. RunKernel consumes it through the
  existing chain; it is not a parallel safety controller.
- `core.ordinary_live_source_custody_runtime` treats selected URL provenance as
  a nontrigger. It validates, but never manufactures, an independently supplied
  current material-need proposal before entering the installed controller.
- The controller also recognizes one provider-neutral `explicit_known_url`
  READ without manufacturing a search-candidate identity; its operation
  identity binds a controller-derived normalized-URL digest.
- `core.ordinary_live_main_runkernel_coverage_runtime` can consume prior custody
  for semantic admission and ComponentCoverage, but cannot reacquire.
- Ordinary Fast, Balanced, Deep/high-complexity, planner-assisted, and recovery
  DISCOVER paths rank only normalized provider-returned material. Historical
  `core.pipeline` selective page fetching and `core.retrieval.fetch_page` /
  `fetch_url_text` are retired.
- Canonical ordinary DISCOVER now populates revision 1 of the existing
  `RunKernel.SearchResultCandidatePacket` from truthful provider-result identity
  and material refs. The selected-candidate packet remains a nontrigger.
- FOCUSED_EXTRACT is mechanically installed and deterministically recognized,
  but the controller returns `focused_extract_requester_not_installed` because
  no current producer supplies exact pre-acquisition focus with complete
  contract/component/obligation lineage.
- No ordinary current-material-need producer reaches READ or Focused Extract.
- No currently cataloged Linkup/Tavily exact-target operation is production
  eligible for untrusted exact URLs.
- Final custody across authorized READ and Focused Extract is not proved.

## Required Build

1. Establish one canonical independent current-material-need producer for the
   actual ordinary post-selection consumer. Candidate selection, URL presence,
   short/absent provider material, weak corpus, complexity, mode, adapter
   installation, and provider availability must remain nontriggers.
2. Where a product-consumed exact URL has no SearchResultCandidate identity,
   consume the installed `explicit_known_url` proposal shape and its
   controller-derived normalized-URL identity. Do not populate `candidate_ref`
   with synthetic candidate IDs or weaken selected-candidate packet membership
   checks.
3. Establish one exact FOCUSED_EXTRACT proposal producer if the same canonical
   material-need owner can truthfully supply its facts. It must bind one to
   twenty exact URLs, exact bounded focus, exact material shape, current
   AnswerContract, current component, and current source obligation.
   The producer may propose only; RunKernel must derive the capability.
4. If current admitted product facts cannot supply that focus, add the smallest
   explicit producer contract to the actual ordinary consumer. Do not derive
   focus from generic mode, complexity, provider features, post-READ display
   anchors, URL presence, or free-form tool instructions. A model-visible
   proposal or prompt contract change requires its own exact schema and the
   semantic validation lane.
5. Route each accepted exact-URL READ or FOCUSED_EXTRACT operation through the
   installed RunKernel capability, work-order, route, execution, terminal, and
   custody actions.
   Preserve `core.routing` as sole provider selector and
   `execute_authorized_acquisition_work_order()` as the only PRODUCT dispatcher.
   Gate 1 must pass before work-order admission, route selection, or active-slot
   reservation. Gate 2 must pass before RunCap charge, execution claim, adapter
   invocation, or transport. Gate 3 must pass after transport but before any
   successful artifact, execution reduction, terminal success, or custody.
6. Extend custody only as needed to consume exact URL-bound READ or Focused
   artifacts. Preserve one source identity per selected URL. Do not aggregate
   unrelated URLs into one source, manufacture search candidates, or let
   provider output create authority.
7. Converge the admitted exact-URL material through existing
   `FetchReadContentPacket` / `SanitizedContentReference`, EvidenceLedger,
   EvidenceRelativeAnalysisPacket, SemanticObservation, and main-RunKernel
   ComponentCoverage owners. Any new Focused handoff must have one named
   ordinary consumer and no shadow final-custody path.
8. Delete, bypass, or explicitly subordinate any superseded post-selection
   exact-URL path. Preserve initial QueryPlan/ProviderPlan ownership, provider-
   returned-material ranking, candidate selection, and the structural zero-
   candidate-URL-transport boundary.
9. Decide and document the default-disabled product flag posture. Offline
   composition may remain injected/default-off, but the result must state
   plainly whether the default CLI consumes it.

## Authority Constraints

- Proposals remain advisory and provider-neutral.
- Provider identity first appears in the accepted route observation.
- Availability may affect routing, never capability.
- Provider target-safety eligibility is a separate pre-dispatch fact from
  capability, availability, code-owned preference, and requester preference.
- One active operation per source obligation and terminal exhaustion remain
  enforced.
- Route-time alternative is allowed only before transport; provider failure
  permits no fallback, retry, or capability switch.
- Requested URL A and an observed resolved, redirected, final, or canonical URL
  B remain separate. B is evaluated independently for network-target safety and
  source-lineage/applicability. Safe applicable B may succeed; safe
  inapplicable B is a lineage/applicability failure; prohibited B is a safety
  failure.
- Fixed provider API endpoints and explicitly authorized local broker/model
  endpoints are outside the untrusted dynamic content-target policy.
- Acquisition and custody do not themselves grant evidence correctness,
  citation eligibility, source-obligation satisfaction, Sufficiency, FAP,
  Author, or answer authority.
- No Map topology, Map-to-URL selection, Crawl page collection, general Deep,
  provider synthesis, new provider, social authority, or UI/conversation work.

## Verification

- Static census proves no ordinary post-selection exact-URL acquisition bypass
  remains and that initial discovery still cannot transport candidate URLs.
- Product tests traverse every RunKernel action exactly once for READ and
  activated Focused Extract, with current contract/component/obligation
  lineage.
- Focused tests prove exact URL/focus identity, one source per URL, stale and
  duplicate rejection, no provider authority in the proposal, and no new model
  call unless an explicitly licensed producer contract requires one.
- Directly affected discovery-boundary, routing, acquisition adapter, candidate packet,
  FetchReadContentPacket, EvidenceLedger, semantic admission, ComponentCoverage,
  and main ordinary composition owners pass offline.
- RunCap fetch/read accounting remains exactly once per attempted transport.
- Zero live calls, zero provider-failure fallback, zero Map/Crawl/Deep
  transports, no local webpage downloader, and no raw/private payload retention.
- Current-state, provider-routing, acquisition-control, and roadmap truth owners
  match the actual default/consumer posture.

## Stop And Split Conditions

If exact focus requires a distinct semantic producer, model-visible proposal
contract, prompt redesign, source-obligation change, or materially different
rollback boundary, stop and return that repository-grounded architecture
decision rather than weakening the controller or fabricating focus.

## Following Checkpoints Remain Queued

No following checkpoint advances while this Build is blocked. After this Build
eventually completes, the next planned checkpoint remains
`PLANNER-DISAMBIGUATION-ACQUISITION-CONVERGENCE-01`. It may connect only
provider-neutral lightweight disambiguation and bounded query-direction hints;
it must not fetch candidate pages or become a second exact-URL authority. Map
selection under
[`SITE-TOPOLOGY-SELECTION-AUTHORITY-01`](SITE_TOPOLOGY_SELECTION_AUTHORITY_01.md)
and Crawl custody remain later. None may be started as part of this PR.
