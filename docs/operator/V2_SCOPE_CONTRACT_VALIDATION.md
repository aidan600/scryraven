# FAILED DEVELOPMENT EXPERIMENT — NOT ACTIVE CANDIDATE SEMANTICS

This is a preserved historical validation record for the scope-contract experiment
at `904ff562e1978265650e5c66f8876b85663a0381`. The experiment did not meet its
semantic objective and was removed from the active V2 candidate during the
2026-09-19 harvest. The detailed text below describes the tested historical tree;
it does not describe current runtime state or authorize compatibility machinery.

# Observation / inference / sufficiency contract repair

OUTCOME: **NOT MET**. The fresh Sol / medium F06 submission is a MATERIAL FAIL.
The specified stop rule was followed: no controls, semantic rerun, or post-failure
contract tuning. The implementation remains an experimental local review surface.

## Before and after

Before, Research's `established` list combined direct source observations and
analytical conclusions. A missing premise could coexist with an answer proposal
at a broader scope, without an explicit scope decision.

After, `observed` and `inferred` replace that list. Both retain exact exposed
Evidence references. `still_needed` also covers missing premises that limit an
inference's permissible scope. Research must obtain a consequential reasonably
obtainable premise or abandon the broader inference and propose a materially
narrower answer. The answer action now declares `answer_scope=requested` with
null `scope_limit`, or `narrowed` with a concise material limitation. Acquisition
decisions leave both fields null. Runtime checks validate declaration shape and
Evidence custody; semantic sufficiency remains the model's responsibility.

This changes the stopping contract, rather than asking for source-to-claim prose
accounting or a relationship label while leaving the same answer decision allowed.
No basis paragraph is required. Answer independently applies the same scope
discipline to actual supplied material. Nevertheless, the live result shows that
this instruction/decision distinction did not prevent unsupported promotion.
It is not evidence of an effective semantic repair.

## Implementation and removal

- `scryraven/v2.py`: replaced the old state, updated reference validation,
  introduced the two Research scope fields and shape checks, and revised both
  semantic prompts in generic terms of population, period, entity, event/version,
  comparison, applicability and conditions.
- `tests/test_v2.py`: migrated the shared experimental decision fixture.
- `tests/test_v2_scope_contract.py`: direct-fact, qualified-rule, legitimate
  technical-synthesis and blocked/narrowed reception states; ordinary missing-
  premise acquisition; exact-reference checks for both lists; malformed scope
  declarations; removed-schema rejection; fresh Answer isolation; prompt semantics.
- `docs/architecture/V2_RESEARCH.md` and `CURRENT.md`: describe the current
  candidate contract, its validation and its limits. This record captures review.

Removal is complete: no old `established` layer or compatibility adapter survives
in the V2 runtime. Historical evidence keeps its original recorded schema.
The shared scoped-item shape remains useful for both lists. The Answer result
schema is unchanged; supported/partial/unable posture already expresses narrower
answers. Its packet receives neither Research observations/inferences nor the
Research scope declaration. There are still exactly two semantic owners.

Search/Read/Find, immutable Evidence, exact views and multi-span readings,
attention, citations, neutral results, sessions, deadlines and no-progress
behavior are unchanged. No new actor, source-count restriction, statistical gate,
provider route, persistent lifecycle or experimental compatibility machinery was
introduced. Scope-contract changes are authorized by the supplied repair brief.

## Tested revision and offline verification

Implementation revision: `904ff562e1978265650e5c66f8876b85663a0381` on
`codex/v2-adaptive-research-01`, starting from
`b76cae9412497c61eabd24de832eccc16e059a89`.

Before any product submission:

- Focused V2 mechanics, acquisition, session, campaign and scope tests: **113 passed**.
- Full suite, including baseline session/citation and timeout regressions:
  **504 passed**.
- Ruff: **passed**. `git diff --check`: **passed**.

The first focused invocation could not write the optional pytest cache, but all
tests passed. The full invocation disabled that cache and completed without the
warning. These tests protect mechanics and transmitted contract instructions;
they do not establish correct model judgment. No runtime or test code changed
after the scored submission. Final edits record validation facts only.

## Frozen live execution

Only `gpt-5.6-sol`, medium reasoning, direct OpenAI Responses API was used.
The disposable caller invokes ordinary `ResearchSession.ask` with the V2 engine,
actual Exa acquisition and only the frozen question. No rubric, expected identity,
source URL, supplied answer, or evaluation case enters the runtime. The existing
credential broker owns secret custody and sanitizes all captured stdout/stderr.

The question/rubric comes unchanged from `V2_CAMPAIGN.md`, manifest SHA-256
`bdcc3c1c7b09b00c8b7dadf45573f5d0d7636b31f7272b792c1ee2d3d27f7e0c`.
This work's pre-execution manifest is
`C:\tmp\scryraven-scope-contract-20260919\MANIFEST.json`, SHA-256
`208fec8e2d659ce2c82bdbc0f6d27133a52833f47ec057eb9e999fcacc0f09ec`.
It freezes code/runner hashes, source rubric, model, run order, attempt rules,
reporting-price assumptions and the ordinary 12 semantic / 16 external / 120-second
limits with 128,000 attention characters.

| Case | Result | Calls | External work | Time | First loss |
| --- | --- | ---: | --- | ---: | --- |
| F06 | MATERIAL FAIL | 6 | 10 searches, 1 full read | 101.813 s | Research inference promotion; requested-scope sufficiency and fresh Answer repeat the overgeneralization |

Consumption: one physical product attempt, one scored submission, zero replacements.
An initial sandbox broker configuration failure had
`target_launch_attempted=false`; it made no product/provider attempt. Its safe
record is retained separately. No independent web verification was performed.

## F06 state and first loss

The correct event was identified as update 0.3.0, released August 29, 2025, with
a requested first-month window through approximately September 28.

1. **Observed:** Research generally retained source attribution, update features,
   direct feedback, journalistic judgments and engagement distinctions. Its
   compact summaries combined source types, but the actual packet preserved them.
   Some forum excerpts lacked full post-date context.
2. **Inferred:** after the first three searches, semantic call 2 already described
   a "cautiously positive, but mixed" account of early update reception while
   acknowledging that the sources were editorials and isolated posts. This is the
   earliest directional promotion beyond bounded source themes. Call 5 made the
   violation explicit: "Reception during the first month was broadly favorable
   relative to the preceding lull." The packet did not establish that comparative
   population/period proposition.
3. **Still needed:** initially retained a broader dated player-reception basis.
   By call 5 it reduced the remaining gap to a precise player-wide approval rate.
   Thus the gap constrained quantification, not the asserted qualitative direction.
4. **Answer scope:** call 5 selected `requested`, with null `scope_limit`; there
   was no narrowed-scope proposal. Additional searches and a full Steambase read
   had not established an update-specific comparison across the requested period.
5. **Fresh Answer:** call 6 returned partial posture and separated journalist
   opinion, individual feedback, whole-game Steam reviews and participation.
   It did not repeat Research's 89.5% net-review calculation or the explicit
   preceding-lull comparison. However, it opened: "The available first-month
   evidence indicates a favorable but clearly mixed reception to The Third Edict."
   Later caveats and a closing reference to sampled early reactions did not retract
   or materially narrow that opening conclusion.

The missing bridge therefore did **not** adequately constrain the final claim.
This judgment does not require a representative survey. A useful qualitative
answer could report recurring campaign/trade praise and performance/endgame
criticism within the reviewed material. The failure is treating those selected
observations as establishing the broader first-month reception direction.

Answer received 14 actual material items; eight were cited. The packet included
official launch/version material, favorable professional reviews, mixed individual
feedback, strongly negative forum/console feedback, activity figures and whole-game
review snapshots. No identified packet omission explains the first loss. Exact
source readings and citations were accepted with zero decision/reading rejections.
The valid partial raised a missing need, then the terminal reservation committed
it without an identical second Answer. The no-progress repair worked mechanically.

## Negative controls and economy

F01, F03 and F05 were **not executed**, as required after F06 failed. Synthetic
direct facts, qualified rules and legitimate two-premise technical synthesis still
complete without an extra inference cycle. Their live behavior under this contract
is unproved; earlier Sol passes on another contract do not establish it.

Usage: 125,380 input tokens, comprising 116,055 ordinary uncached, 7,460 cached
and 1,865 cache-write tokens; 6,564 output tokens, including 1,208 reasoning
tokens. The model estimate is **$0.607809**, using the prior local campaign's
frozen $4/$0.40/$5/$20 per-million ordinary/cached/write/output schedule.
This is a reporting assumption, not a reverified current price or a provider bill.
Acquisition cost and provider-reported monetary cost are unavailable. No cost gate
or optimization experiment was used.

## Evidence preservation, current truth and consumption

Exact sanitized execution files, the frozen manifest, attempt ledger, adjudication
and usage summary remain in `C:\tmp\scryraven-scope-contract-20260919\`.
The useful failure is also preserved with hashes in ignored
`local-evals/candidates/sol-f06-observation-inference-scope-failure-20260919/`
and indexed as a DEVELOPMENT candidate. It contains the exact question, answer,
all acquired Evidence, the complete Answer packet, cited material, normalized
observations, safe trace, citations, usage and evaluation. It is not held-out or
adjudicated gold. No evidence cleanup was performed.

The supplied brief and preserved Luna, Sol and Sonnet F06 records informed this
repair, together with the historical source-to-claim and relationship-label
no-signal results. No separate cross-model forensic document was found in the
inspected local artifact locations; no missing artifact was reconstructed.

`CURRENT.md` is updated. Production remains Research -> Analyst -> Author on Luna
/ medium, with the default Reading Room path unchanged. V2 remains experimental
and unpublished. The repair leaves the candidate understandable and cleanly
materializable from production main at the previously reviewed integration
boundaries: it changes V2-local state/prompts and their tests, without new coupling
or compatibility machinery. This does not imply adoption readiness, and the full
consumption audit was not repeated.

## Handoff

Human/ChatGPT review must decide the disposition of this failed semantic repair:
retain it as experimental development evidence or authorize a different revision.
No additional live work, wider campaign, publication, merge or adoption phase is
started. The ordinary checkout stays on the existing experimental branch; local
commits only. `main` is unchanged, and no PR was created.
