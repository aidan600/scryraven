# Mainline Semantic Supersession 01

## Outcome and decision

**Met.** The ordinary application now implements the accepted clean-room
Research -> mechanical Search / Read / Find -> reassessment -> fresh Evidence-first
Answer architecture. The old Analyst checkpoint and separate Author handoff are
removed from the executable product tree. Offline integration, ordinary compatibility,
durable follow-up and a counted Level-5 dependency passed. The first Anker run
preserved the requested sufficiency restraint but did not establish the conversion-
rule obligation in the frozen Foundation 02 rubric. That result remains incomplete;
it is not relabeled or erased.

One explicitly authorized unchanged confirmation satisfied the complete frozen
L4C obligation by acquiring and citing FAA's volts-times-ampere-hours rule. No
semantic code, prompt, acquisition policy or model configuration changed. This is
bounded acceptance with observed acquisition variability, not deterministic
retrieval reliability. No other accepted live obligation was reopened or rerun.
The completed branch is for one PR into `main`; publication is not merge authority.
Human review remains required before any merge. No Level-6 work was begun.

## Verified starting truth

- Starting branch: `codex/luna-capability-recursive-01b`.
- Exact starting HEAD: `b72defe1fa05568175a6a39a91b858a0783d6504`.
- Clean working tree; no unrelated changes.
- Local `main`, `origin/main`, and remote production `main` all verified at
  `533f45df9271978e0591fb35aa8da2b1917255b2`, an ancestor of the accepted HEAD.
- Implementation branch: `codex/mainline-semantic-supersession-01`, created from
  that accepted HEAD; `main` was not changed.
- Live-tested implementation checkpoint:
  `d544cef780186f63e08d29d390653e84fd279f05`. Subsequent changes are this evidence
  record and `CURRENT.md` only; no untested final runtime change.
- Focused-closure starting/tested HEAD:
  `97c3c77aa501a9cf0d37c2a4e21f9fde37455cae`, clean, on the same branch. Only
  factual documentation differed from the live-tested runtime checkpoint.

The accepted Research runtime matched the harvested
`725ef54fc929731c6f78f5d563bc7dc32bbcf732` state. Its later lineage changes were
campaign tooling/tests and current/baseline documentation, not a new Research or
Answer architecture. Foundation 01, Foundation 02, Recursive 01 and Recursive 01B
manifests, adjudications and exact public packets remain in their existing
`C:\tmp\scryraven-luna-*-20260919` directories. Their hashes were recorded before
integration. Stopped Investigator and earlier lab branches, including
`codex/investigator-control-repair-01` and
`codex/research-loop-architecture-lab-01`, remain intact. No evaluation evidence
or historical branch was deleted.

## Responsibility and final-state diff

| Surface | Before on production main | Supersession candidate |
| --- | --- | --- |
| `scryraven/research.py` | Orientation/navigation/relevance, Analyst coverage and `next_need`, separate Author from Analysis | Accepted `v2.py` Research and fresh Answer loop promoted directly; only mechanical names/imports change |
| `scryraven/acquisition.py` | Acquisition intertwined with old orchestration and `SourcePackets` | Accepted `v2_acquisition.py` Search/Read/Find mechanics; unchanged URL helpers moved from old engine |
| `scryraven/results.py` | Old `Result` requires Analysis | Native candidate `CompletedAnswer`, exact evidence/citations, no generated Analysis |
| `scryraven/session.py` | Old result and semantic-history context | Native new result; fresh decisions over retained actual acquisitions; conversation for referents only |
| `scryraven/session_store.py` and `historical.py` | Required old Analysis-bearing turn | Nullable Analysis for native turns; decode-only original Analysis validation for historical records |
| CLI and Reading Room | Old ordinary engine, candidate CLI selector in experimental lineage | Default isolated/session/browser entrypoints share the same promoted application; `--v2` removed |
| `scryraven/model.py` | Research/Analyst/Author roles and old cache phase boundaries | Research/Answer labels and mechanically corresponding exact-content cache boundaries |

Removed active semantic surfaces include `Orientation`, `ResearchAction`,
`RelevantEvidence`, the old `Result`/`Draft`, `ANALYST_PROMPT`, `AUTHOR_PROMPT`,
old session semantic prompts, `_research`, `_relevant_evidence`,
`_followup_need`, `_author_result`, and Analyst-driven `next_need` orchestration
in the former engine. Old `SourcePackets` and the
Investigator executable harness/Author compatibility adapter are absent. Tests
whose promises were only that old split were removed; surviving source, session,
failure, presentation and cache promises were retained or rewritten.

Promoted surfaces are the accepted `ResearchDecision`, compact understanding,
`AnswerDecision` with literal source readings, `run`/`_run_turn`,
`AcquisitionLibrary`, and `CompletedAnswer`/citation mechanics. Research chooses
acquisition and stopping; Answer receives the request and actual selected sources,
not generated Research findings or a draft as authority. There is no new semantic
owner, fallback engine, runtime architecture selector or added product restriction.
The session's existing dependency-injection seam is used by offline tests only;
there is no second product engine behind it.

An AST comparison against the accepted HEAD verified all promoted Research/Answer
functions/classes after the mechanical class, stage and cache-name substitutions.
Acquisition, citation and surviving source mechanics matched, and moved URL helpers
matched the old code. Prompt SHA-256 values (UTF-8, normalized newlines) are unchanged:

- Research: `28c4acac582393b2e54470b3659218f830a6d0692ba9ec72e0520728d98ca495`
- Answer: `26ae39ac61553034a32ca20cc96ed19543262de9d0e73d2ef78f2fab84642dc7`

Model/provider/search policy did not change: GPT-5.6 Luna / medium, existing Exa
transport, 12 semantic attempts, 16 external attempts, 120 seconds, 128,000
attention characters. The credential doorman and provider transport were unchanged.
Legacy SMART environment/configuration names remain readable for compatibility but
no current semantic stage selects that role. No package, CLI or environment rename
was introduced.

## Historical sessions and custody

The existing SQLite version-1 snapshot is backward readable. New turns serialize
`analysis: null`; actual historical Analysis objects remain intact. Old judgments
are neither Evidence nor context for new decisions. Historical decode validation
does not call models and reopening does not rewrite the saved payload. A synthetic
pre-supersession snapshot was generated through the actual old ordinary engine and
serializer before replacement. Tests reopen it, preserve its exact record and
append a native turn. No user session database was inspected or migrated.

Immutable acquisitions, canonical URL/source identity, provider-highlight/full-
source/view provenance, exact selected-material snapshots and deterministic compact
citations remain. Existing transactional revision checks, failure rollback,
rename/delete/reopen, safe rendering/security and presentation operate on native
turns. General very-long-session behavior remains unproved.

## Offline and browser evidence

- Before live work: 99 focused accepted-candidate/acquisition/session/review/campaign
  tests passed, establishing the starting candidate.
- Final implementation suite: **308 passed**. Coverage includes focused Research /
  Answer behavior, exact material/citation custody, native and historical sessions,
  follow-ups, rollback/conflict/corruption handling, CLI, Reading Room/server,
  provider/model transport, prompt caching and campaign observation isolation.
  The final command used `--basetemp
  C:\tmp\scryraven-mainline-semantic-supersession-01\pytest-final-02` after sandbox
  permissions prevented clearing the old default temporary directory. All tests
  passed there; an optional pytest cache-write permission warning remained.
- The real CLI/transport adapter test observes exactly
  `research, research, answer`, Luna / medium, actual acquired source content,
  no Analysis/semantic-history input and no Research working state in Answer.
  Ordinary session tests establish the same stages on reopened retained Evidence.
- Full Ruff/static checks, normal pre-commit checks and `git diff --check` passed.
  Full final diff was inspected against both the accepted HEAD and production main.
- Synthetic browser acceptance used the ordinary Reading Room application/session
  path, offline model/provider injection and isolated Edge through Playwright.
  Existing 1440-, 1920- and 390-pixel layouts passed without horizontal overflow.
  Citation dock/sheet, exact historical versions/views, full expansion, safe original
  links, history/focus, new/follow-up forms and disabled working state passed.
  Partial/unable turns committed; a failed turn preserved prior history and editable
  input. Rename, actual server restart/reopen, delete cancellation and confirmation
  passed. No page errors, external browser requests or live research calls occurred.

Browser screenshots and the acceptance report are in the external phase packet.
Browser harness synchronization was corrected locally before its passing run; no
product repair was needed. Initial pre-commit environment setup issues (Git TLS,
safe-directory context and writable virtualenv data) were corrected with process-
local settings. The sole secret-scanner annotation covers a synthetic credential-
URL rejection fixture, not a real credential.

## Ordinary live evidence

The first five rows used revision `d544cef780186f63e08d29d390653e84fd279f05`, GPT-5.6 Luna /
medium, the unchanged candidate limits and existing credential broker. Exact
authorized questions were used. Only the question went to runtime; frozen
evaluator obligations and expected answers were not supplied.
The confirmation row used `97c3c77aa501a9cf0d37c2a4e21f9fde37455cae` with the same
unchanged runtime, model and limits.

| Case / question | Result | Semantic attempts | External acquisition | Public trajectory / demonstrated purpose |
| --- | --- | --- | --- | --- |
| A: JWST partners, launch, orbit, mirror, wavelengths | PASS, supported | 3 | 1 Search | NASA/ESA/CSA; December 25, 2021; heliocentric halo near L2; approximately 6.5 m; red/near-to-mid infrared. NASA material supports the combined answer. Multi-need compatibility demonstrated. |
| B1: four BIPM SI prefixes added in 2022 | PASS, supported | 3 | 1 Search | BIPM resolution/news -> ronna R 10^27, quetta Q 10^30, ronto r 10^-27, quecto q 10^-30. Six actual materials persisted in an ordinary CLI-created durable session. |
| B2: which two are smaller than one | PASS, supported | 3 | 0 | Separate CLI resume -> fresh Research -> local Read of retained E2 -> fresh Answer: ronto/r and quecto/q. E2 was acquired but uncited in B1. Exact B1 record and six acquisitions unchanged after reopening. |
| C: Anker published capacity by itself and FAA 100 Wh threshold | INCOMPLETE frozen obligation; supported posture | 3 | 2 Searches | Correctly refuses mAh/output-only inference and distinguishes separately published 99.54 Wh from the FAA threshold. Voltage-dependent conversion rule is not established by Answer or captured cited material. Central restraint observed; full frozen Level-4 control not demonstrated. |
| G1: GE takeoff-thrust range for NASA/GE modified base engine | PASS, supported, dependency exercised | 4 | 4 Searches | Anchor GE/NASA material -> Passport becomes concrete -> Passport-specific acquisition -> GE datasheet -> 17,325–18,920 lb. Ordinary Level-5 witness demonstrated. |
| C confirmation: exact frozen Anker L4C question | PASS, supported | 3 | 2 Searches | Anker mAh capacity + FAA threshold and volts-times-Ah formula -> fresh Answer rejects mAh-only sufficiency and treats independently acquired 99.54 Wh separately. |

G1's exact frozen Recursive 01B question was:

> What takeoff-thrust range does GE publish for the base engine NASA and GE modified for their hybrid-electric engine demonstration?

The first Research route's two queries did not name Passport. Actual E4/E12
identified the modified base engine; the next public decision identified the missing
GE-published Passport thrust range and changed the searches accordingly. Downstream
E13/E19 supplied the range. Fresh Answer cited E4/E13 and distinguished an older
E15 rating. This was an exercised dependency, not a correct shortcut. G2 and G7 were
not needed and were not submitted.

The frozen Foundation 02 L4C obligation remains unchanged: establish the FAA Wh
threshold and conversion rule, preserve "by itself", and do not treat mAh as
directly comparable to Wh without voltage even when additional product facts are
found. The first promoted-product answer preserves the central distinction, but its cited
Anker and FAA material lacks the conversion rule. CLI capture retained exact final
cited material, not all other exposed sources: E1 was also exposed to Answer but
was not cited and its exact body is not in this capture. No missing text was
reconstructed. Therefore this first-run record makes neither a full frozen PASS claim nor
a claim that no other acquired source contained the rule. The acceptance gap is
semantic/evidentiary, not an identified deterministic integration defect.

Totals including focused closure: **6 ordinary submissions**, **19 semantic
attempts** (13 Research, 6 Answer), **10 external Searches**, **0 external Reads**,
**1 local Read**, **0 Finds**. The continuation used exactly **1 additional
submission**, **3 semantic calls**, **2 Searches**, and **0 premium calls**.
No independent external adjudication calls or semantic/integration repairs occurred.
There was exactly one explicitly authorized same-code Anker confirmation; no other
case was rerun. The original nine-submission ceiling was not exhausted, but this
continuation's single-run authorization is complete. Runtime emitted supported
posture in every case; that label does not change the first Anker evaluator result.

## Focused Anker acceptance closure

Exact question, unchanged from the frozen Foundation 02 manifest:

> Does Anker's published capacity for the Prime Power Bank (27K, 250W), by itself, establish that it falls under the FAA's 100 Wh no-airline-approval threshold?

The ordinary `scryraven.research.run` entrypoint received only that question and
a passive public observation callback. No model, limit, prompt, URL, fact, rubric,
route or engine override was supplied. The existing credential doorman captured
sanitized output. The target completed successfully and used only ordinary
GPT-5.6 Luna / medium Research and Answer calls.

| Acquired material | Identity and role | Frozen obligation |
| --- | --- | --- |
| E1 | Anker US `https://www.anker.com/products/a1340-250w-power-bank`; A1340 Prime 27K/250W, 27,650 mAh, 250W maximum output | Relevant product and mAh capacity established |
| E7 | FAA `https://www.faa.gov/hazmat/packsafe/airline-passengers-and-batteries`; 0–100 Wh framework, larger-battery approval and carry-on/airline qualifications | Applicable threshold established |
| E10 | FAA `https://www.faa.gov/hazmat/packsafe/resources/spare-batteries-packing-poster-1920x1080`; explicitly multiply volts by ampere-hours to determine Wh | Ah-unit requirement and voltage dependency established |
| E11 | FAA `https://www.faa.gov/hazmat/packsafe/lithium-batteries`; same volts-times-Ah rule and 100 Wh framework | Corroborating acquired material, supplied to Answer |
| E5 | Anker NZ `https://www.anker.com/nz/products/a1340-250w-power-bank`; separate 99.54 Wh specification for this model | Independent Wh fact, not inferred from mAh alone |

All twelve acquired materials are preserved exactly with provider-highlight
provenance. E1/E5/E7/E10/E11 reached fresh Answer; E1/E5/E7/E10 were finally cited.
Answer's literal E10 reading matches the exact acquired passage and offsets.
The FAA formula requires ampere-hours rather than the published milliampere-hours,
and independently requires volts. This is materially equivalent authoritative
wording for the necessary conversion relationship. The excerpts do not separately
print a divide-by-1000 instruction; no such quotation or numerical mAh-to-Wh
calculation is claimed. The sufficiency answer does not require an assumed battery
voltage, and no output-port voltage was substituted for it.

The exact central Answer conclusion was: "neither figure, by itself, establishes
the battery’s watt-hours", followed by the FAA volts-times-Ah explanation and the
need for voltage or an explicit Wh rating. It then introduced Anker's 99.54 Wh as
a separate published fact and retained FAA/airline qualifications. The complete
frozen obligation therefore **passes**; there is no consequential loss in this
confirmation.

This differs from the preserved first incomplete trajectory specifically in the
acquired and cited conversion-rule support. It does not erase that trajectory or
establish deterministic acquisition reliability. No semantic repair was required.
The full offline suite, Ruff, pre-commit and diff checks passed again after the
documentation-only closure; runtime identity against `d544cef...` remained intact.

## Evidence preservation and limits

The external phase packet is
`C:\tmp\scryraven-mainline-semantic-supersession-01`. It contains safe CLI traces,
exact cited public source text, normalized G1 acquisition/exposure events, answers,
broker completion statuses, dedicated public BIPM validation snapshots, accounting,
semantic-equivalence evidence and browser acceptance. No unrelated database, raw
private provider payload, credential or hidden reasoning was captured.

Useful controls/boundaries were additionally captured as development candidates:

- `mainline-supersession-01-anker-20260920`
- `mainline-supersession-01-bipm-20260920`
- `mainline-supersession-01-passport-20260920`
- `mainline-supersession-01-anker-confirmation-20260920`

They live in ignored `local-evals/candidates/`, with exact-artifact hashes and
provenance, and do not replace previous gold. No evaluation packet, manifest or
corpus is a runtime dependency. `scripts/v2_campaign.py` remains an explicitly
development-only ordinary-session observer; historical experiment documents remain
snapshots. Investigator/lab executables remain available in preserved Git history.

Foundation 01's valid Levels 1–3 cases, Foundation 02's three prior Level-4 cases
and Recursive 01B's three prior counted Level-5 cases establish bounded development
evidence only. This supersession adds an ordinary JWST control, durable BIPM reuse,
one exercised ordinary Level-5 witness and complete Anker acceptance after one
prior incomplete acquisition trajectory, with the variability limitation above.
Aggregate/open-world sufficiency, general reliability and very long sessions are
not established. Literal readings/citation validation establish custody and
membership, not semantic entailment. **Level 6 revision/recovery under superseding
Evidence is not established.** No Level-6 work was started.
